The TypeScript server package has been deprecated.

The Go core package in `core` now owns the Bastion host API service and CLI entrypoint. See [Core](./core.md) for the active service and CLI layout.
